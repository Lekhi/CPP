#include <iostream>

using namespace std;

int main() {

   
   int zmienna = 5;
    int * zmienna_ptr = &zmienna;
    int **wskaznik_na_wskaznik = &zmienna_ptr;
    int ** array [2] { wskaznik_na_wskaznik, &zmienna_ptr };

    for(int i = 0; i < 2; i++){
        cout<<**array[i]<<endl;
    }

   return 0;
}
